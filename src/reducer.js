const defaultState = {
    teachers: []

}


// takes two arguments :previous state and action
export default function reducer(prevState = defaultState, action){
    switch(action.type){
        default:
            return prevState
    }
}

